env: dev
spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 
    url: jdbc:mysql://127.0.0.1:3306/weixindiancan?useCharactorEncoding=utf-8&useSSL=false&serverTimezone=UTC
  jpa:
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8888/eureka/
  instance:
    prefer-ip-address: true
    
server:
  port: 8002
